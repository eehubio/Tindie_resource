'use client';

// src/components/BottomTabBar.tsx
// Disabled globally: this app is embedded into tindie.com and should not show
// its own bottom navigation on any page. Kept as a null-returning component so
// existing imports in the layout continue to work.

export default function BottomTabBar() {
  return null;
}
