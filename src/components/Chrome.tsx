// Chrome is intentionally disabled: this app is embedded into tindie.com,
// which provides the real header and footer. Rendering nothing here removes
// all duplicate chrome across every page in one place.

export function TindieHeader() {
  return null;
}

export function TindieFooter() {
  return null;
}
